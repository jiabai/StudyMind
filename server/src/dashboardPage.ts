export type DashboardAccountView = { authenticated: true; email: string; entitlement_status: "active" | "inactive"; entitlement_expires_at: string | null; llm_quota_limit: number; llm_quota_used: number; llm_quota_remaining: number; llm_quota_resets_at: string | null; llm_configured: boolean; last_verified_at: string; can_process: boolean; can_generate_ai: boolean };
const esc = (value: unknown) => String(value ?? "").replace(/[&<>'"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" })[char]!);
export function safeJsonForScript(value: unknown): string { return JSON.stringify(value).replace(/</g, "\\u003c").replace(/\u2028/g, "\\u2028").replace(/\u2029/g, "\\u2029"); }
export function renderDashboardPage(input: { account: DashboardAccountView; csrfToken: string }) { return `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>StudyMind 控制台</title></head><body><main><h1>StudyMind 控制台</h1><p id="email">${esc(input.account.email)}</p><p>处理权限：${input.account.can_process ? "可用" : "不可用"}</p><button id="logout">退出登录</button></main><script>const csrf=${safeJsonForScript(input.csrfToken)};document.querySelector("#logout").onclick=()=>fetch("/user/auth/logout",{method:"POST",headers:{"x-studymind-csrf":csrf}})</script></body></html>`; }
